import { io } from "socket.io-client";
import React from "react";
import { SOCKET_URI } from "../config/uri";

// export const socket = io(SOCKET_URI);
// export const SocketContext = React.createContext();